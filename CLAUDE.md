# Claude Code Instructions

## Project Overview
This is `claude-clean`, a Go CLI tool for cleaning Git repositories.

## Development Workflow

When completing tasks, follow this checklist:

1. Make necessary code changes
2. Run tests if applicable: `make test`
3. **If Go files were modified, run `make fmt` to format the code**
4. Verify the build: `make build`
